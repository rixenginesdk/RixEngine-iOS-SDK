//
//  AlxLevelPlayMetaInfo.swift
//  AlxAdsDemo
//
//  LevelPlay (Unity IronSource) 适配器元信息 / LevelPlay (Unity IronSource) Adapter Meta Information
//

import Foundation

struct AlxLevelPlayMetaInfo {
    /**
     * 适配器版本号。
     * Adapter version number.
     */
    static let ADAPTER_VERSION = "1.0.0"
    /**
     * 网络名称（与 LevelPlay 平台注册时的名称一致）。
     * Network name (must match the name registered on the LevelPlay platform).
     */
    static let NETWORK_NAME = "AlxAds"
}
